# change these values as you see fit
# after running `sudo ./install`, the server should run at http://host:port/base_url
host = '127.0.0.1'
port = 5000
base_url = 'files'
username = 'admin'
password = 'mypassword'
realm = 'My Server'

# for example, with above default values, the server should run at 127.0.0.1:5000/files
